#include <iostream>
#include "menu.h"

int main() {

    int gameCode = mainMenu(0);
    // (maybe) vector<Characters> setup = gameSetup(gameCode);
    // (maybe) int result = combat(setup);
    return 0;
}